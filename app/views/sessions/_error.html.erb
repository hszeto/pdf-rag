<%# Never a dead end: the message always arrives with a way to try again (R7.5).
    The landing screen below carries the file field, which is that retry. %>
<div role="alert" class="mb-6 rounded border p-4 text-lg">
  <p><%= message %></p>
</div>
