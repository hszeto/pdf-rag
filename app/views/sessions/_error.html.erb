<%# Never a dead end: the message always arrives with a way to try again (R7.5). %>
<div role="alert" class="bg-alert-bg text-alert-ink rounded-lg p-5 mb-8">
  <p class="text-lead font-bold"><%= message %></p>
</div>
